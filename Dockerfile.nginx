FROM nginx:alpine

# Copy nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copy public directory from app
COPY public /var/www/html/public

# Expose port 80
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
